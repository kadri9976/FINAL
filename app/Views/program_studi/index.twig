<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data Program Studi</title>
    <link rel="stylesheet" href="/FINAL/public/assets/css/style.css">
</head>
<body>
    <div class="container">
        <div class="page-header">
            <h2>🎓 Data Program Studi</h2>
            <p>Kelola semua program studi yang tersedia</p>
        </div>

        <div class="btn-group">
            <a href="/FINAL/public/program-studi/create" class="btn btn-primary">+ Tambah Program Studi</a>
            <a href="/FINAL/public/" class="btn btn-secondary">← Kembali ke Beranda</a>
        </div>

        {% if programStudi %}
        <div class="table-wrapper">
            <table>
                <thead>
                    <tr>
                        <th style="width: 25%;">Kode Program Studi</th>
                        <th style="width: 50%;">Nama Program Studi</th>
                        <th style="width: 25%;">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    {% for ps in programStudi %}
                    <tr>
                        <td><strong>{{ ps.kode_program_studi }}</strong></td>
                        <td>{{ ps.nama_program_studi }}</td>
                        <td>
                            <div class="action-buttons">
                                <a href="/FINAL/public/program-studi/edit/{{ ps.kode_program_studi }}" class="btn-edit">✏️ Edit</a>
                                <a href="/FINAL/public/program-studi/delete/{{ ps.kode_program_studi }}" class="btn-delete" onclick="return confirm('Yakin ingin menghapus?')">🗑️ Hapus</a>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
        {% else %}
        <div class="table-wrapper">
            <div class="empty-state">
                <div class="empty-state-icon">📭</div>
                <h3>Tidak ada data program studi</h3>
                <p>Mulai dengan menambahkan program studi baru</p>
            </div>
        </div>
        {% endif %}
    </div>
</body>
</html>
